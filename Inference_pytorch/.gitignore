# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# NeuroSIM build artifacts
NeuroSIM/*.o
NeuroSIM/main

# Default run logs (omit unless you explicitly need them in git)
log/default/

# Only these two benchmark logs are tracked; ignore everything else here
logrun/*
!logrun/VGGcifar10Compare.log
logrun2/*
!logrun2/cifar10_vgg8_sigmadelta_paperDefaults_Vdd0p4_rowModeledNotCounted_clkDecoupled.log
